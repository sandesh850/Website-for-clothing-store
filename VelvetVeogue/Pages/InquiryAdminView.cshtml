@page
@model VelvetVeogue.Pages.InquiryAdminViewModel
@{
}

<div class="container">

    <!--Main row-->
    <div class="row" style="margin-top:5%">

        <div class="col-12 col-md-12">

            <table class="table">
                <thead>

                    <tr>

                      @*   <th scope="col">Image</th> *@
                        <th scope="col"><strong>Subject</strong></th>
                        <th scope="col"><strong>Message</strong></th>
                       @*  <th scope="col"><strong>Price</strong></th>
                        <th scope="col"><strong>Color</strong></th>
                        <th scope="col"><strong>Size</strong></th>
                        <th scope="col"><strong>Address</strong></th>
                        <th scope="col"><strong>CNO</strong></th>
                        <th scope="col"><strong>Email</strong></th>
                        <th scope="col"><strong>Date</strong></th>
                        <th scope="col"><strong>Action</strong></th> *@


                    </tr>

                </thead>

                <tbody>

                    @foreach (var data in Model.Inquiriestbl)
                    {
                        <tr>
                           @*  <td>
                                @if (data.img != null && data.img.Length > 0)
                                {
                                    <img src="data:image/png;base64,@Convert.ToBase64String(data.img)" style="width:80px;height:80px;object-fit:cover" alt="Item Image" />
                                }
                                else
                                {
                                    <span>No Image Found</span>
                                }

                            </td> *@
                            <td>@(data.subject)</td> 
                            <td>@(data.Message)</td>
                           @*  <td>@(data.price.ToString() ?? "N/A")</td>
                            <td>@(data.color)</td>
                            <td>@(data.sizes ?? "N/A")</td>
                            <td>@(data.address ?? "N/A")</td>
                            <td>@(data.contactNo)</td>
                            <td>@(data.email)</td>
                            <td>@(data.date.ToShortDateString())</td>

                            <!--Action Buttons-->
                            <td>

                                <div class="d-flex">

                                    <a asp-page="completeOrders" asp-route-id="@data.id" class="btn btn-warning">Complete</a>
                                  

                                </div>

                            </td>
 *@
                        </tr>

                    } 

                </tbody>
            </table>

        </div>

    </div>

</div>